/// This duration in milliseconds
const kBounceAnimDuration = 150;

/// This interval in milliseconds
const kLiveListAnimInterval = 70;

/// This duration in milliseconds
const kDefaultAnimationDuration = 350;

enum Status { pure, loading, success, empty, error, finished,initial }

